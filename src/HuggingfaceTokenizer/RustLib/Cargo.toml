[package]
name = "tokenize"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
tokenizers = "0.23"
libc = "0.2"
lazy_static = "1.5"

[lib]
crate-type = ["cdylib"]
